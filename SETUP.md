# MECH-FORGE Setup Instructions

## 🚨 Essential Prerequisite: Node.js

This project is a Next.js web application, which requires **Node.js** to run.
Currently, Node.js was not detected on your system.

### Steps to Start:

1.  **Install Node.js**:
    -   Download the "LTS" version from [https://nodejs.org/](https://nodejs.org/)
    -   Run the installer and ensure "Add to PATH" is checked.
    -   **Restart your terminal/VS Code** for changes to take effect.

2.  **Install Dependencies**:
    Open a terminal in this folder (`c:\NAVEEN\hack\MECH-FORGE`) and run:
    ```bash
    npm install
    ```
    This will download all the necessary tools (Next.js, React, Tailwind, etc.).

3.  **Run the App**:
    ```bash
    npm run dev
    ```
    Open [http://localhost:3000](http://localhost:3000) in your browser.

---

### Project Structure (Generated)
-   `app/`: Main application code.
-   `components/`: UI components (Shadcn).
-   `lib/`: Utility functions.
-   `globals.css`: Custom theme variables (Mechanical/Dark).
